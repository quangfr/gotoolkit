{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome (Serve public)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5000",
      "webRoot": "${workspaceFolder}/public",
      "preLaunchTask": "Serve public"
    }
  ]
}
